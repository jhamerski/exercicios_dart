class Brasil {
  final double latitude;
  final double longitude;
  static const String capital = 'Brasília';
  static const estados = ['SP', 'SC', 'RS', '...'];

  const Brasil(this.latitude, this.longitude);
}